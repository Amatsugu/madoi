[package]
name = "madoi"
version = "0.1.0"
edition = "2024"

[dependencies]
nannou = "0.19.0"
serde = { version = "1.0.228", features=[ "derive"]}
serde_json = "1.0.150"

[features]
default = ["dev"]
dev = []
